# Declaration Files

This section is intended to expose Babylon Toolkit declarations and DLC runtime classes, including systems such as camera controllers, player controllers, input, and scene helpers. Use these declaration files as the source of truth so developers and AI agents can glean which classes exist and what properties, methods, and function signatures are available before generating implementation code.
